export default function CategoriesPage() {
  return (
    <section aria-labelledby="categories-title" className="w-full max-w-2xl">
      <h1
        id="categories-title"
        className="text-heading-sm text-text font-medium"
      >
        Categorías
      </h1>
      <p className="text-body text-text-muted mt-3">
        La gestión de categorías y etiquetas se conectará cuando el catálogo
        esté disponible.
      </p>
    </section>
  );
}
